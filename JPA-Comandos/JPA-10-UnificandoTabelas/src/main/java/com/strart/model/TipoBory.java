package com.strart.model;

import jakarta.persistence.Entity;
import jakarta.persistence.Table;
/*
@Entity
@Table(name = "tipo_bory")
public class TipoBory {


    // construtores
    public TipoBory() {}
    
    public TipoBory(String nome, int limite) {
        this.nome = nome;
        this.limite = limite;}

    // regra de negócio
    public boolean limiteAtingido() {
        return quantidadeCards >= limite;
    }

    public void incrementarCard() {
        if (limiteAtingido()) {
            throw new IllegalStateException("Limite de cards atingido para " + nome);
        }
        this.quantidadeCards++;
    }
}
*/
